name: generate animation

on:
  # har 24 soatda avtomatik yangilanadi
  schedule:
    - cron: "0 */24 * * *" 
  
  # istalgan payt qo'lda ishga tushirish imkoniyati
  workflow_dispatch:
  
  # har safar main branchga push bo'lganda
  push:
    branches:
    - main

jobs:
  generate:
    permissions: 
      contents: write
    runs-on: ubuntu-latest
    timeout-minutes: 5
    
    steps:
      # iloncha animatsiyasini generatsiya qilish
      - name: generate github-contribution-grid-snake.svg
        uses: Platane/snk/svg-only@v3
        with:
          github_user_name: ${{ github.repository_owner }}
          outputs: |
            dist/github-contribution-grid-snake.svg
            dist/github-contribution-grid-snake-dark.svg?palette=github-dark
          
      # tayyor bo'lgan faylni "output" branchiga yuklash
      - name: push github-contribution-grid-snake.svg to the output branch
        uses: crazy-max/ghaction-github-pages@v3.1.0
        with:
          target_branch: output
          build_dir: dist
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
